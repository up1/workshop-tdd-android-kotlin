# workshop-tdd-android-kotlin

```
// List all gradle's tasks
$gradlew tasks

// Run unit tests
$gradlew tests

// Run android tests
$gradlew connectedAndroidTest

// Generate code coverage
$gradlew clean jacocoTestReport
```
